@extends('partials.nav')
{{--
<div class="container">
    <div class="row">
        @foreach($places as $place)
        <h2>{{ $place->name }}</h2>
        <p>{{ $place->description }}</p>
        <a href="{{ route('place.show', $place->id) }}" class="btn btn-primary">View More Details</a>
    @endforeach

    </div>
</div> --}}
